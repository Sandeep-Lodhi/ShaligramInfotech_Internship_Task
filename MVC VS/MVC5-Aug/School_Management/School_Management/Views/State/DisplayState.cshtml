@*@model IEnumerable<School_Management.Models.Model.StateCustomModel>*@

@model IEnumerable<School_Management.Models.Context.StateList_Result>

@{
    ViewBag.Title = "DisplayState";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DisplayState</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered bg-gradient">
    <tr>
        @*<th>
                @Html.DisplayNameFor(model => model.StateId)
            </th>*@
        <th>
            @Html.DisplayNameFor(model => model.StateName)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.CountryId)
            </th>*@
        <th>
            @Html.DisplayNameFor(model => model.CountryName)
        </th>
        <th>Action</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.StateId)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.StateName)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.CountryId)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.CountryName
           )
            </td>
            <td>
                @Html.ActionLink("Edit", "EditState", new { id = item.StateId }) |
                @Html.ActionLink("Details", "Details", new { id = item.StateId }) |
                <a href="/State/DeleteStateRecord/@item.StateId">Delete</a>
                @*@Html.ActionLink("Delete", "DeleteStateRecord", new { id = item.StateId })*@
            </td>
        </tr>
    }

</table>
